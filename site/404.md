---
layout: page
title: 404 — 页面不存在
---

<script setup>
// 404 page is shared across locales (VitePress emits a single 404.html).
// Detect the locale from the URL and show matching copy/links.
const isEn = typeof window !== 'undefined' && (window.location.pathname === '/en' || window.location.pathname.startsWith('/en/'))
const home = isEn ? '/en/' : '/'
const kb = isEn ? '/en/kb/ctf-website/README' : '/kb/ctf-website/README'
const title = isEn ? 'Page not found' : '页面不存在'
const desc = isEn
  ? 'The address you visited does not exist or has moved. Start again from the home page or the knowledge base.'
  : '你访问的地址不存在或已移动。试试从首页或知识库入口重新开始。'
const homeLabel = isEn ? 'Back to home' : '返回首页'
const kbLabel = isEn ? 'Enter Knowledge Base' : '进入知识库'
</script>

<div style="max-width: 640px; margin: 96px auto; text-align: center">
  <p style="font-size: 4rem; font-weight: 700; margin: 0; letter-spacing: -0.03em; font-family: var(--rl-font-display)">404</p>
  <h1 style="font-size: 1.25rem; margin: 16px 0 8px">{{ title }}</h1>
  <p style="color: var(--rl-ink-soft)">{{ desc }}</p>
  <p style="margin-top: 24px">
    <a :href="home" style="display: inline-block; padding: 8px 20px; background: var(--rl-primary); color: #fff; border-radius: 10px; text-decoration: none">{{ homeLabel }}</a>
    <a :href="kb" style="display: inline-block; margin-left: 12px; padding: 8px 20px; border: 1px solid var(--rl-line); border-radius: 10px; text-decoration: none">{{ kbLabel }}</a>
  </p>
</div>
